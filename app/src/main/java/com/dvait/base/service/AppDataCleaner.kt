package com.dvait.base.service

object AppDataCleaner {
    private const val TAG = "AppDataCleaner"

    /**
     * Cleans captured text based on the source application's package name.
     */
    fun cleanText(text: String, packageName: String): String {
        return text
    }
}
